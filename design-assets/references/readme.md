# UI Reference Components

These components are reference implementations only.

Purpose:
- Study animation techniques
- Study interaction patterns
- Study layout composition

Do NOT copy these directly into RestaurantOS.

Instead:
- Adapt the interaction
- Match RestaurantOS design tokens
- Match RestaurantOS component architecture
- Match RestaurantOS accessibility standards